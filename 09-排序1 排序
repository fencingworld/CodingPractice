#include <iostream>
#include <vector>
#include <stdio.h>
#include <algorithm>
using namespace std;

int main()
{
    int n,t;
    cin>>n;
    vector<int>x;
    for (int i=0;i<n;i++)
    {
        //cin>>t;
        scanf("%d",&t);
        x.push_back(t);
    }
    sort(x.begin(),x.end());
    int i;
    for (i=0;i<n-1;i++)
    {
        printf("%d ",x[i]);
        //cout<<x[i]<<" ";
    }
printf("%d\n",x[i]);
    //cout << x[i]<< endl;
    return 0;
}
